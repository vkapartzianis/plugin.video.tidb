<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<addon id="plugin.video.tidb.kine"
       name="TheIntroDB Kine Addon"
       version="3.0.1"
       provider-name="TheIntroDB">
    <requires>
        <import addon="xbmc.python" version="3.0.1"/>
    </requires>
    <extension point="xbmc.service" library="service.py" start="startup"/>
    <extension point="xbmc.addon.metadata">
        <path>plugin.video.tidb.kine-3.0.1.zip</path>
        <summary lang="en_GB">Skip intro, recap, credits, and preview segments in movies and TV shows using TheIntroDB</summary>
        <description lang="en_GB">Looks up segment timings from TheIntroDB while you watch. Uses TMDB ids when present; otherwise IMDb ids (tt…) with season/episode.</description>
        <disclaimer lang="en_GB">This product uses the TMDB API but is not endorsed or certified by TMDB.</disclaimer>
        <platform>all</platform>
        <license>GPL-2.0-or-later</license>
        <assets>
            <icon>resources/icon.png</icon>
        </assets>
        <news lang="en_GB">v3.0.1 - Performance: the two segment databases are now queried in parallel (one round-trip instead of two), and request rate limiting is tracked per service so a backoff for one host never throttles the other.</news>
    </extension>
</addon>
